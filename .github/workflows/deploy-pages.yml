name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - "src/web/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: deploy-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_BASE_PATH: /marvel-rivals-team-composer
      # Optional CORS proxy for the profile-import feature; the UI stays
      # hidden while this repo variable is unset (see infra/rivalsmeta-proxy.js).
      NEXT_PUBLIC_PROFILE_PROXY: ${{ vars.PROFILE_PROXY_URL }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: src/web/package-lock.json

      - name: Install dependencies
        working-directory: src/web
        run: npm ci

      - name: Test
        working-directory: src/web
        run: npm run test

      - name: Build static export
        working-directory: src/web
        run: npm run build

      - uses: actions/upload-pages-artifact@v5
        with:
          path: src/web/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
