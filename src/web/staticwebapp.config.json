{
  "navigationFallback": { "rewrite": "/index.html", "exclude": ["/api/*"] },
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"],
      "rewrite": "http://localhost:7071/api/{*path}"
    }
  ]
}
